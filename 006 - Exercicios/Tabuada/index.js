//const multiplicador = parseInt(prompt("Digite um número para multiplicar"));
const multiplicador =7;

for(let i = 0; i <=10; i++){
    const calc = multiplicador * i;
    console.log(`${multiplicador} * ${i} = ${calc}`); // ${} usado para interpolar variáveis
} 